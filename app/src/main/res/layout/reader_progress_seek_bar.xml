<?xml version="1.0" encoding="utf-8"?><!-- 现代化阅读器底部栏：渐变遮罩 + 上滑设置提示 + 材质滑块 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/reader_progress_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentBottom="true"
    android:background="@drawable/bg_reader_bottom_scrim"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingTop="4dp"
    android:paddingEnd="16dp"
    android:paddingBottom="8dp"
    android:visibility="gone">

    <!-- 上滑手柄提示区：点击或上滑均可打开快捷设置面板 -->
    <LinearLayout
        android:id="@+id/reader_swipe_hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingTop="6dp"
        android:paddingBottom="2dp">

        <View
            android:layout_width="36dp"
            android:layout_height="4dp"
            android:background="@drawable/bg_reader_sheet_handle" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:drawablePadding="4dp"
            android:fontFamily="sans-serif-medium"
            android:text="@string/reader_settings_swipe_up"
            android:textColor="#B3FFFFFF"
            android:textSize="11sp" />
    </LinearLayout>

    <com.xyrlsz.xcimocob.ui.widget.SeekBar
        android:id="@+id/reader_seek_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:stepSize="1"
        android:value="1"
        android:valueTo="100"
        app:labelStyle="@style/SliderTooltip" />
</LinearLayout>